<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (HTTP) is permitted only for local development hosts:
  the Android emulator loopback (10.0.2.2) and localhost. Every other
  destination — including the production NoBrainFit server — must use HTTPS.

  To test against a local server/Ollama from a physical device on the same
  Wi-Fi, add your machine's LAN IP (e.g. 192.168.1.42) as a <domain> below.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
